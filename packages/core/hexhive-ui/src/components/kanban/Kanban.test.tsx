import React from 'react';
import { Kanban } from './Kanban'
import { shallow } from 'enzyme'

describe('Kanban', () => {

    it('Mounts', () => {
        const wrapper = shallow(<Kanban />)

    })
})