import React from 'react'

import { UploadOption, Update, FormFolder  } from "grommet-icons"


export const ACTIONS = [{key: 'upload', icon: UploadOption}, {key: 'convert', icon: Update }, {key: 'organise', icon: FormFolder }]